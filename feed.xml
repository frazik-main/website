<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://frazik.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://frazik.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/feed.xml</id><title type="html">Frazik</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2010-11-04-design-pattern-template/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2010-11-04-design-pattern-template</id><content type="html" xml:base="https://frazik.com/blog/2025/2010-11-04-design-pattern-template/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Factory design patterns belong to the group of patterns known as “creational.” These patterns assist in the instantiation of classes, thereby separating the object creation process from its usage. (While creating new classes isn’t exactly like low-level memory allocation for pointers, this analogy highlights the underlying abstraction.) The name “factory” is derived from the concept of a “factory of objects.”</p> <p>Instead of directly using the <code class="language-plaintext highlighter-rouge">new</code> keyword for object creation, this pattern provides an alternative. With factory patterns, you can choose which concrete class to instantiate at runtime. This also allows you to introduce new classes into your code without modifying the existing base code.</p> <p>This concept largely boils down to common sense applications of polymorphism. Remember: Always program to an interface, not a concrete class!</p> <p>I will use the same example to demonstrate both patterns. In this example, we need to decorate a string with special characters. For instance, “decorate me” will become “** decorate me **” by adding two asterisks at both ends. All my examples are kept simple to focus on the core concept and the problem at hand.</p> <h2 id="factory-method">Factory Method</h2> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 * Simple example of Factory Method pattern.
 * @author jan.krizan
 */</span>
<span class="kd">interface</span> <span class="nc">Decorator</span> <span class="o">{</span>

    <span class="nc">String</span> <span class="nf">doDecorating</span><span class="o">(</span><span class="nc">String</span> <span class="n">str</span><span class="o">);</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">StarDecorator</span> <span class="kd">implements</span> <span class="nc">Decorator</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">doDecorating</span><span class="o">(</span><span class="nc">String</span> <span class="n">str</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"** "</span> <span class="o">+</span> <span class="n">str</span> <span class="o">+</span> <span class="s">" **"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">OtherDecorator</span> <span class="kd">implements</span> <span class="nc">Decorator</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">doDecorating</span><span class="o">(</span><span class="nc">String</span> <span class="n">str</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"&lt;&lt;|-- "</span> <span class="o">+</span> <span class="n">str</span> <span class="o">+</span> <span class="s">" --|&gt;&gt;"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Factory</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="kt">int</span> <span class="no">STAR_DECORATOR</span> <span class="o">=</span> <span class="mi">1</span><span class="o">;</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="kt">int</span> <span class="no">OTHER_DECORATOR</span> <span class="o">=</span> <span class="mi">2</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="nc">Decorator</span> <span class="nf">createSpecificDecorator</span><span class="o">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">i</span> <span class="o">==</span> <span class="no">STAR_DECORATOR</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nf">StarDecorator</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">else</span> <span class="k">if</span> <span class="o">(</span><span class="n">i</span> <span class="o">==</span> <span class="no">OTHER_DECORATOR</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nf">OtherDecorator</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>

        <span class="nc">String</span> <span class="n">nonDecoratet</span> <span class="o">=</span> <span class="s">"Please decorate me!"</span><span class="o">;</span>

        <span class="nc">Decorator</span> <span class="n">decorator</span> <span class="o">=</span> <span class="n">createSpecificDecorator</span><span class="o">(</span><span class="no">STAR_DECORATOR</span><span class="o">);</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">decorator</span><span class="o">.</span><span class="na">doDecorating</span><span class="o">(</span><span class="n">nonDecoratet</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>The output of this example is: “** Please decorate me! **”. Let’s break down the code:</p> <ul> <li><strong>Lines 5-8:</strong> We first create an interface, <code class="language-plaintext highlighter-rouge">Decorator</code>.</li> <li><strong>Lines 10-15 and 17-22:</strong> Then, we implement that interface with <code class="language-plaintext highlighter-rouge">StarDecorator</code> and <code class="language-plaintext highlighter-rouge">OtherDecorator</code>. As you can see, this is straightforward so far.</li> <li><strong>Lines 29-37:</strong> This section represents the factory that chooses which class to instantiate based on the input parameter.</li> <li>The <code class="language-plaintext highlighter-rouge">main</code> method then tests this functionality.</li> </ul> <h2 id="abstract-factory">Abstract Factory</h2> <p>As its name suggests, the Abstract Factory pattern operates at a higher level of abstraction. In this pattern, even the factory itself is abstract (either an interface or an abstract class), and we choose the specific factory at runtime, not the “working” class directly. This chosen factory then “knows” which classes it needs to instantiate to fulfill the task at hand.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 *
 * Simple example of Abstract Factory pattern.
 * @author jan.krizan
 */</span>
<span class="kd">interface</span> <span class="nc">DecoratorFactory</span> <span class="o">{</span>

    <span class="nc">Decorator</span> <span class="nf">createDecorator</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">StarDecoratorFactory</span> <span class="kd">implements</span> <span class="nc">DecoratorFactory</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">Decorator</span> <span class="nf">createDecorator</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">StarDecorator</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">OtherDecoratorFactory</span> <span class="kd">implements</span> <span class="nc">DecoratorFactory</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">Decorator</span> <span class="nf">createDecorator</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nf">OtherDecorator</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">interface</span> <span class="nc">Decorator</span> <span class="o">{</span>

    <span class="nc">String</span> <span class="nf">doDecorating</span><span class="o">(</span><span class="nc">String</span> <span class="n">str</span><span class="o">);</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">StarDecorator</span> <span class="kd">implements</span> <span class="nc">Decorator</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">doDecorating</span><span class="o">(</span><span class="nc">String</span> <span class="n">str</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"** "</span> <span class="o">+</span> <span class="n">str</span> <span class="o">+</span> <span class="s">" **"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">class</span> <span class="nc">OtherDecorator</span> <span class="kd">implements</span> <span class="nc">Decorator</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">doDecorating</span><span class="o">(</span><span class="nc">String</span> <span class="n">str</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="s">"&lt;&lt;|-- "</span> <span class="o">+</span> <span class="n">str</span> <span class="o">+</span> <span class="s">" --|&gt;&gt;"</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Factory</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="kt">int</span> <span class="no">STAR_DECORATOR</span> <span class="o">=</span> <span class="mi">1</span><span class="o">;</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="kt">int</span> <span class="no">OTHER_DECORATOR</span> <span class="o">=</span> <span class="mi">2</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="nc">DecoratorFactory</span> <span class="nf">createSpecificDecorator</span><span class="o">(</span><span class="kt">int</span> <span class="n">i</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">i</span> <span class="o">==</span> <span class="no">STAR_DECORATOR</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nf">StarDecoratorFactory</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">else</span> <span class="k">if</span> <span class="o">(</span><span class="n">i</span> <span class="o">==</span> <span class="no">OTHER_DECORATOR</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nf">OtherDecoratorFactory</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>

        <span class="nc">String</span> <span class="n">nonDecoratet</span> <span class="o">=</span> <span class="s">"Please decorate me!"</span><span class="o">;</span>

        <span class="nc">DecoratorFactory</span> <span class="n">decoratorFac</span> <span class="o">=</span> <span class="n">createSpecificDecorator</span><span class="o">(</span><span class="no">OTHER_DECORATOR</span><span class="o">);</span>

        <span class="nc">Decorator</span> <span class="n">decorator</span> <span class="o">=</span> <span class="n">decoratorFac</span><span class="o">.</span><span class="na">createDecorator</span><span class="o">();</span>

        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">decorator</span><span class="o">.</span><span class="na">doDecorating</span><span class="o">(</span><span class="n">nonDecoratet</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>You’ll notice that this code is quite similar to the Factory Method example. However, there are small but important differences:</p> <ul> <li><strong>Lines 11-16 and 18-23:</strong> Here, we introduce another layer of abstraction: <code class="language-plaintext highlighter-rouge">DecoratorFactory</code> and its concrete implementations. While this might seem like unnecessary code for this simple example, it can be beneficial if, for instance, you have more than one method to implement, and you don’t want all concrete classes to implement all methods (only a relevant subset). In this way, your concrete classes implement only the methods required for their specific work and can ignore others. You can extend interfaces to achieve this. If this sounds a bit confusing, I recommend searching for more comprehensive examples of the Abstract Factory pattern to see its full potential, for example, <a href="http://www.javacamp.org/designPattern/">here</a>.</li> </ul> <p>Creating class instances and testing are similar processes. The primary difference is the additional step of obtaining the <code class="language-plaintext highlighter-rouge">Decorator</code> instance through the <code class="language-plaintext highlighter-rouge">createDecorator</code> method of the <code class="language-plaintext highlighter-rouge">DecoratorFactory</code>.</p> <p>While using the Abstract Factory pattern for such a simple example might seem like overkill, my intention is to clearly illustrate the differences between the two patterns.</p> <p>You can download the code for these two examples from <a href="http://code.google.com/p/codingwithpassionblog/source/browse/#svn/trunk/src/org/codingwithpassion/patterns%3Fstate%3Dclosed">here</a>.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
</code></pre></div></div>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2011-09-25-groovy-diff-java-1/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2011-09-25-groovy-diff-java-1</id><content type="html" xml:base="https://frazik.com/blog/2025/2011-09-25-groovy-diff-java-1/"><![CDATA[<h2 id="what-is-groovy-all-about">What is Groovy all about?</h2> <p><strong>Groovy</strong> is a <a href="http://en.wikipedia.org/wiki/Dynamic_programming_language">dynamic programming language</a> that runs on the JVM. This means it is compiled to Java bytecode and then executed on the Java Virtual Machine. You can read more about its features and download Groovy from the <a href="http://groovy.codehaus.org/">official website</a>.</p> <p>After unpacking or installing Groovy, you can use the Groovy console (located at <code class="language-plaintext highlighter-rouge">$GROOVY_HOME/bin/groovyConsole</code>) for writing and experimenting with Groovy scripts.</p> <p>Groovy aims to provide greater productivity compared to Java (and other mainstream object-oriented languages), and it excels at this.</p> <p>A simple “hello world” in Groovy (no <code class="language-plaintext highlighter-rouge">main</code> method or anything required):</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">println</span> <span class="s2">"Hello programmers!"</span>
</code></pre></div></div> <h2 id="basic-differences">Basic Differences</h2> <p>Almost all Java code is valid Groovy code, allowing for seamless mixing of Java and Groovy within your projects.</p> <ul> <li>Groovy provides more default imports than Java, including <code class="language-plaintext highlighter-rouge">java.io</code>, <code class="language-plaintext highlighter-rouge">java.math</code>, <code class="language-plaintext highlighter-rouge">java.util</code>, <code class="language-plaintext highlighter-rouge">java.net</code>, <code class="language-plaintext highlighter-rouge">groovy.lang</code>, and <code class="language-plaintext highlighter-rouge">groovy.util</code>. This is because a significant percentage of Java classes utilize these packages.</li> <li> <p>In Groovy conditional statements, both <code class="language-plaintext highlighter-rouge">null</code> values and empty collections evaluate to <code class="language-plaintext highlighter-rouge">false</code>. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Map</span> <span class="n">map</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">();</span>
<span class="c1">// In Java you must do following for example:</span>
<span class="k">if</span> <span class="o">(</span><span class="n">map</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">map</span><span class="o">.</span><span class="na">size</span><span class="o">()</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
  <span class="c1">// ...</span>
<span class="o">}</span>
<span class="c1">// In Groovy this is enough:</span>
<span class="k">if</span> <span class="o">(!</span><span class="n">map</span><span class="o">)</span> <span class="o">{</span>
  <span class="c1">// ...</span>
<span class="o">}</span>
</code></pre></div> </div> <p>Similarly, an empty string also evaluates to <code class="language-plaintext highlighter-rouge">false</code>.</p> </li> <li><code class="language-plaintext highlighter-rouge">public</code> is the default visibility for classes, fields, constants, and methods in Groovy, contrasting with Java’s package-private default visibility.</li> <li>Unlike Java, where the compiler forces you to catch <code class="language-plaintext highlighter-rouge">checked exceptions</code>, Groovy does not require this. While this can lead to cleaner code and remove a sense of false security, the decision of whether to catch these exceptions depends on your specific problem domain.</li> </ul> <h2 id="silent-helpers">Silent Helpers</h2> <p>Groovy offers several features that enhance productivity by removing unnecessary syntax from Java equivalents and replacing it with smart defaults.</p> <ul> <li><strong>Semicolons</strong>: In Groovy, semicolons are generally optional. However, if you place multiple statements on a single line, you must separate them with semicolons.</li> <li><strong>Parentheses</strong>: While generally required, parentheses are optional in a few specific cases, such as with the <code class="language-plaintext highlighter-rouge">println</code> command or closures (which will be discussed in the second part of this tutorial).</li> <li> <p><strong><code class="language-plaintext highlighter-rouge">return</code> statement</strong>: In non-void methods, the <code class="language-plaintext highlighter-rouge">return</code> statement is often optional. Groovy automatically returns the result of the last evaluated expression. For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kt">boolean</span> <span class="nf">something</span><span class="o">()</span> <span class="o">{</span>
    <span class="s2">"a"</span> <span class="o">==</span> <span class="s2">"b"</span>
<span class="o">}</span>
<span class="c1">// Will return "false".</span>
</code></pre></div> </div> <p>However, it is often advisable to use the <code class="language-plaintext highlighter-rouge">return</code> statement explicitly for better code readability.</p> </li> </ul> <h2 id="properties-fields">Properties (fields)</h2> <p>Groovy provides field access notation for Java bean properties (or fields). It automatically generates getters and setters for private fields behind the scenes. To leverage this, simply leave the default visibility on your fields. For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Date</span> <span class="n">date</span> <span class="o">=</span> <span class="k">new</span> <span class="n">Date</span><span class="o">()</span>
<span class="c1">// You can access all Java bean properties</span>
<span class="c1">// using their field name.</span>
<span class="n">println</span> <span class="n">date</span><span class="o">.</span><span class="na">time</span>
<span class="c1">// prints some long number...</span>

<span class="kd">class</span> <span class="nc">MyClass</span> <span class="o">{</span>

    <span class="c1">// Default visibility for fields is private,</span>
    <span class="c1">// but Groovy generates setters and getters</span>
    <span class="c1">// behind the scenes.</span>
    <span class="n">String</span> <span class="n">firstName</span>
    <span class="n">String</span> <span class="n">lastName</span>

    <span class="c1">// Compiles without problem.</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">access</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">MyClass</span> <span class="n">myClass</span> <span class="o">=</span> <span class="k">new</span> <span class="n">MyClass</span><span class="o">()</span>
        <span class="n">myClass</span><span class="o">.</span><span class="na">firstName</span> <span class="o">=</span> <span class="s2">"John"</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <h2 id="constructors">Constructors</h2> <p>In Groovy, you can instantiate beans using a <strong>list of named arguments</strong>. This powerful feature allows you to effectively treat maps as objects that can be passed around. For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Here we create a new SimpleDateFormat and set default</span>
<span class="c1">// values to some of its properties.</span>
<span class="n">SimpleDateFormat</span> <span class="n">format</span> <span class="o">=</span>
    <span class="k">new</span> <span class="nf">SimpleDateFormat</span><span class="o">(</span><span class="nl">lenient:</span> <span class="kc">false</span><span class="o">,</span> <span class="nl">numberFormat:</span> <span class="s2">"ddMMyyyy"</span><span class="o">)</span>
</code></pre></div></div> <h2 id="added-operations">Added Operations</h2> <p>Groovy introduces several new operators not found in Java:</p> <ul> <li> <p><strong>Null-safe operator (<code class="language-plaintext highlighter-rouge">?.</code>)</strong>: This operator provides null-safe object navigation, similar to a null-checked dot (<code class="language-plaintext highlighter-rouge">.</code>) operator. For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// In Java, if you have an object named "obj" that</span>
<span class="c1">// encapsulates an object named "field", you must do</span>
<span class="c1">// the following to check if "field" is null:</span>
<span class="k">if</span> <span class="o">(</span><span class="n">obj</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">obj</span><span class="o">.</span><span class="na">field</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
  <span class="c1">// ...</span>
<span class="o">}</span>

<span class="c1">// In Groovy, you can use the null-safe operator</span>
<span class="c1">// for the same check and get the same result:</span>
<span class="k">if</span> <span class="o">(</span><span class="n">obj</span><span class="o">?.</span><span class="na">value</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
  <span class="c1">// ...</span>
<span class="o">}</span>
</code></pre></div> </div> <p>This operator helps avoid <code class="language-plaintext highlighter-rouge">java.lang.NullPointerException</code>s, making code safer and cleaner.</p> </li> <li> <p><strong>Elvis operator (<code class="language-plaintext highlighter-rouge">?:</code>)</strong>: This is a shortened version of Java’s ternary (if-then-else) operator. It’s playfully called the ‘Elvis Operator’ due to its resemblance to Elvis Presley’s trademark hair! For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// When using the ternary operator in Java,</span>
<span class="c1">// you normally code something like this:</span>
<span class="n">Date</span> <span class="n">date</span> <span class="o">=</span> <span class="o">(</span><span class="n">row</span><span class="o">.</span><span class="na">getDateFromDatabase</span><span class="o">()</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">?</span>
             <span class="n">row</span><span class="o">.</span><span class="na">getDateFromDatabase</span><span class="o">()</span> <span class="o">:</span> <span class="k">new</span> <span class="n">Date</span><span class="o">();</span>
<span class="c1">// In Groovy, you can get the same result by using the Elvis operator:</span>
<span class="n">Date</span> <span class="n">date</span> <span class="o">=</span> <span class="n">row</span><span class="o">.</span><span class="na">getDateFromDatabase</span> <span class="o">?:</span> <span class="k">new</span> <span class="n">Date</span><span class="o">()</span>
<span class="c1">// It automatically checks for null values.</span>
</code></pre></div> </div> </li> <li> <p><strong>Spread-dot operator (<code class="language-plaintext highlighter-rouge">*.</code>)</strong>: This operator calls a method on every item within a collection. For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This will create a new List with all employees' salaries.</span>
<span class="n">List</span> <span class="n">employees</span> <span class="o">=</span> <span class="n">allEmployees</span><span class="o">*.</span><span class="na">getSalary</span><span class="o">()</span>
</code></pre></div> </div> <p>This eliminates the need to loop through a list simply to extract single properties into another list, offering a more concise and Groovy way of handling collections!</p> </li> <li> <p><strong>Spaceship operator (<code class="language-plaintext highlighter-rouge">&lt;=&gt;</code>)</strong>: This operator is particularly useful when implementing the <code class="language-plaintext highlighter-rouge">Comparable</code> interface. In Java, correctly implementing the <code class="language-plaintext highlighter-rouge">int compare</code> method (returning negative if the first value is smaller, positive if bigger, or zero otherwise) requires careful attention to the API specification. Groovy simplifies this with the spaceship operator, which performs exactly this comparison:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This method will properly compare</span>
<span class="c1">// (in correspondence to Java API) two integers.</span>
<span class="kd">public</span> <span class="kt">int</span> <span class="nf">compare</span><span class="o">(</span><span class="kt">int</span> <span class="n">number1</span><span class="o">,</span> <span class="kt">int</span> <span class="n">number2</span><span class="o">)</span> <span class="o">{</span>
  <span class="k">return</span> <span class="n">number1</span> <span class="o">&lt;=&gt;</span> <span class="n">number2</span>
<span class="o">}</span>
</code></pre></div> </div> </li> <li> <p><strong>Equals operator (<code class="language-plaintext highlighter-rouge">==</code>)</strong>: In Groovy, <code class="language-plaintext highlighter-rouge">==</code> is equivalent to Java’s <code class="language-plaintext highlighter-rouge">equals()</code> method. It is null-safe, meaning it will not throw a <code class="language-plaintext highlighter-rouge">NullPointerException</code> when dealing with null values. It’s important to note that if an object implements the <code class="language-plaintext highlighter-rouge">Comparable</code> interface, <code class="language-plaintext highlighter-rouge">==</code> will use the <code class="language-plaintext highlighter-rouge">compareTo()</code> method rather than <code class="language-plaintext highlighter-rouge">equals()</code>. For identity comparison, you can use the <code class="language-plaintext highlighter-rouge">is()</code> method.</p> </li> </ul> <h2 id="basic-types-in-groovy">Basic Types in Groovy</h2> <p>In Groovy, you have access to all primitive Java types, along with some Groovy-specific types. Additionally, you can benefit from Groovy’s syntactic sugar when working with basic types.</p> <ul> <li> <p><strong>Numbers</strong>: Groovy treats primitive types as objects, allowing you to call methods on them. For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This is perfectly legal in Groovy</span>
<span class="mi">222222</span><span class="o">.</span><span class="na">abs</span><span class="o">()</span>
<span class="c1">// Although not so useful in this particular</span>
<span class="c1">// example.</span>
</code></pre></div> </div> <p>Groovy uses <code class="language-plaintext highlighter-rouge">BigDecimal</code> for floating-point calculations. While potentially slower than <code class="language-plaintext highlighter-rouge">long</code> and <code class="language-plaintext highlighter-rouge">double</code>, it eliminates the common quirks and precision issues associated with <code class="language-plaintext highlighter-rouge">float</code> and <code class="language-plaintext highlighter-rouge">double</code>.</p> </li> <li> <p><strong>Strings</strong>: While Java <code class="language-plaintext highlighter-rouge">String</code>s can be used, Groovy offers several advantages, most notably <strong>embedded Groovy expressions</strong> within strings. Anything enclosed within <code class="language-plaintext highlighter-rouge">${}</code> will be evaluated and interpolated into the string.</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">amount</span> <span class="o">=</span> <span class="mi">255</span>
<span class="n">Date</span> <span class="n">today</span> <span class="o">=</span> <span class="k">new</span> <span class="n">Date</span><span class="o">()</span>

<span class="n">String</span> <span class="n">out</span> <span class="o">=</span> <span class="s2">"Something, something, ${today} is amount: ${amount}"</span>

<span class="n">println</span> <span class="n">out</span>
<span class="c1">// This will print the following:</span>
<span class="c1">// Something, something, Sun Sep 25 18:34:52 CEST 2011 is amount: 255</span>
</code></pre></div> </div> <p>This eliminates the need for clumsy Java string concatenation, improving readability and conciseness.</p> <p><strong>Multiline strings (<code class="language-plaintext highlighter-rouge">"""</code>)</strong> are another Groovy extension. They allow you to easily create string literals that span multiple lines and include line breaks.</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// So this is how you used to do this in Java:</span>
<span class="n">String</span> <span class="n">bla1</span> <span class="o">=</span> <span class="s2">"Something, something,..."</span> <span class="o">+</span> <span class="s2">"\n\n"</span> <span class="o">+</span>
              <span class="s2">"              Another thing...."</span>
<span class="c1">// Same code in Groovy:</span>
<span class="n">String</span> <span class="n">bla1</span> <span class="o">=</span> <span class="s2">"""Something, something,...

                Another thing...."""</span>
</code></pre></div> </div> <p>This avoids the need for <code class="language-plaintext highlighter-rouge">+ "\n" +</code> concatenation for multiline strings.</p> </li> <li> <p><strong><code class="language-plaintext highlighter-rouge">as</code> operator</strong>: This is a powerful casting operator (similar to Java’s <code class="language-plaintext highlighter-rouge">(String) someObject</code>) but with extended capabilities. It can cast <code class="language-plaintext highlighter-rouge">String</code>s to <code class="language-plaintext highlighter-rouge">Number</code>s (and vice-versa), <code class="language-plaintext highlighter-rouge">List</code>s to <code class="language-plaintext highlighter-rouge">Array</code>s (and vice-versa), <code class="language-plaintext highlighter-rouge">Set</code>s to <code class="language-plaintext highlighter-rouge">List</code>s or <code class="language-plaintext highlighter-rouge">Array</code>s (and vice-versa), and even <code class="language-plaintext highlighter-rouge">String</code>s to <code class="language-plaintext highlighter-rouge">List</code>s (and vice-versa). For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="n">number</span> <span class="o">=</span> <span class="o">(</span><span class="s2">"12345"</span> <span class="k">as</span> <span class="kt">int</span><span class="o">)</span> <span class="o">-</span> <span class="mi">1</span>
<span class="n">println</span> <span class="n">number</span>
<span class="c1">// 12344</span>
<span class="kt">boolean</span> <span class="n">isEqual</span> <span class="o">=</span> <span class="mi">123</span> <span class="k">as</span> <span class="n">String</span> <span class="o">==</span> <span class="s2">"123"</span>
<span class="c1">// true</span>
<span class="n">println</span> <span class="n">isEqual</span>
<span class="n">List</span> <span class="n">abcList</span> <span class="o">=</span> <span class="s2">"abc"</span> <span class="k">as</span> <span class="n">List</span>
<span class="n">println</span> <span class="n">abcList</span>
<span class="c1">// [a, b, c]</span>
</code></pre></div> </div> <p>These features are precisely what one would expect from a dynamic scripting language and are quite powerful.</p> </li> </ul> <h2 id="maps-lists-and-ranges">Maps, Lists, and Ranges</h2> <p>Groovy integrates list and map concepts more naturally into its type system.</p> <ul> <li> <p><strong>Lists and Maps</strong>: Groovy offers a more natural syntax for defining Lists and Maps compared to Java. It uses comma-separated sequences of items enclosed in square brackets for definition. Let’s look at some examples:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// In Java</span>
<span class="n">List</span> <span class="n">cars1</span> <span class="o">=</span> <span class="k">new</span> <span class="n">ArrayList</span><span class="o">();</span>
<span class="n">cars1</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s2">"Citroen"</span><span class="o">);</span>
<span class="n">cars1</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s2">"Audi"</span><span class="o">);</span>
<span class="c1">// [Citroen, Audi]</span>

<span class="c1">// In Groovy</span>
<span class="n">List</span> <span class="n">cars2</span> <span class="o">=</span> <span class="o">[</span><span class="s2">"Citroen"</span><span class="o">,</span> <span class="s2">"Audi"</span><span class="o">]</span>
<span class="c1">// [Citroen, Audi]</span>

<span class="c1">// Maps in Groovy (you know the Java part ;) )</span>
<span class="n">Map</span> <span class="n">map1</span> <span class="o">=</span> <span class="o">[</span><span class="s2">"amount"</span> <span class="o">:</span> <span class="mi">15</span><span class="o">,</span> <span class="s2">"size"</span> <span class="o">:</span> <span class="mi">5</span><span class="o">]</span>

<span class="c1">// Empty list</span>
<span class="n">List</span> <span class="n">l</span> <span class="o">=</span> <span class="o">[]</span>
<span class="c1">// Empty map</span>
<span class="n">Map</span> <span class="n">m</span> <span class="o">=</span> <span class="o">[:]</span>

<span class="c1">// Get element from List</span>
<span class="n">String</span> <span class="n">car</span> <span class="o">=</span> <span class="n">cars2</span><span class="o">[</span><span class="mi">1</span><span class="o">]</span>

<span class="c1">// Get element from Map</span>
<span class="n">String</span> <span class="n">amount</span> <span class="o">=</span> <span class="n">map1</span><span class="o">[</span><span class="s2">"amount"</span><span class="o">]</span>

<span class="c1">// Add item to list (somewhat different)</span>
<span class="n">cars2</span> <span class="o">&lt;&lt;</span> <span class="s2">"Ferrari"</span>
<span class="c1">// [Citroen, Audi, Ferrari]</span>

<span class="c1">// Add item to map</span>
<span class="n">map1</span><span class="o">[</span><span class="s2">"mass"</span><span class="o">]</span> <span class="o">=</span> <span class="mi">55</span>
</code></pre></div> </div> </li> <li> <p><strong>Ranges</strong>: Ranges are related to lists and represent a combination of a lower and upper bound. In the case of an integer range, this includes all numbers between the two bounds (inclusive). For example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">List</span> <span class="n">computerComponents</span> <span class="o">=</span> <span class="o">[</span><span class="s2">"Monitor"</span><span class="o">,</span> <span class="s2">"CPU"</span><span class="o">,</span> <span class="s2">"GPU"</span><span class="o">,</span> <span class="s2">"Mem"</span><span class="o">,</span> <span class="s2">"HDD"</span><span class="o">]</span>
<span class="c1">// [Monitor, CPU, GPU, Mem, HDD]</span>

<span class="c1">// Here you define only a subset of the previous list.</span>
<span class="n">List</span> <span class="n">subComponents</span> <span class="o">=</span> <span class="n">computerComponents</span><span class="o">[</span><span class="mi">1</span><span class="o">..</span><span class="mi">2</span><span class="o">]</span>
<span class="c1">// [CPU, GPU]</span>
</code></pre></div> </div> </li> </ul> <h2 id="regular-expressions">Regular Expressions</h2> <p>Groovy provides first-class support for regular expressions at the language level, introducing new literals, operators for matching, and a concise syntax for extracting groups. While regular expressions are a complex topic on their own and beyond the full scope of this introductory article, it’s important to note that Groovy uses forward slashes (not backslashes, like Java) for defining regular expressions. It also introduces two new operators for matching:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">=~</code></strong>: This operator checks if the pattern on the right side can be <em>found anywhere</em> within the string on the left side. It returns a <code class="language-plaintext highlighter-rouge">Matcher</code> object, which evaluates to <code class="language-plaintext highlighter-rouge">true</code> in a boolean context if a match is found.</li> <li><strong><code class="language-plaintext highlighter-rouge">==~</code></strong>: This operator checks if the string on the left side <em>exactly matches</em> the entire pattern on the right side. It returns <code class="language-plaintext highlighter-rouge">true</code> for an exact match, <code class="language-plaintext highlighter-rouge">false</code> otherwise.</li> </ul> <p>Example:</p> <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">println</span> <span class="s2">"999999"</span> <span class="o">==~</span> <span class="s">/\d+/</span>
<span class="c1">// true</span>

<span class="n">println</span> <span class="s2">"mercury9"</span> <span class="o">==~</span> <span class="s">/\d+/</span>
<span class="c1">// false</span>

<span class="n">println</span> <span class="s2">"mercury"</span> <span class="o">==~</span> <span class="s">/\w+/</span>
<span class="c1">// true</span>
</code></pre></div></div> <h2 id="what-is-groovy-missing">What is Groovy missing</h2> <p>Groovy is not missing much, but some features not present (or different) from Java include:</p> <ul> <li>Character literals.</li> <li>Java-like <code class="language-plaintext highlighter-rouge">for</code> loop (specifically, the lack of a comma operator for multiple initializations/increments).</li> <li><code class="language-plaintext highlighter-rouge">do...while</code> loops.</li> <li>Inner and anonymous classes (though closures, to be covered in Part 2 of this tutorial, and the ability to declare multiple classes in a single Groovy file, mitigate this).</li> </ul>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2012-11-21-unmerge-svn/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2012-11-21-unmerge-svn</id><content type="html" xml:base="https://frazik.com/blog/2025/2012-11-21-unmerge-svn/"><![CDATA[<h2 id="how-to-revert-to-a-previous-svn-revision">How to Revert to a Previous SVN Revision</h2> <p>If you ever need to revert a <code class="language-plaintext highlighter-rouge">trunk</code> or any other directory in Subversion (SVN) to a previous revision, or effectively remove a specific revision’s changes, simply perform a <strong>reverse merge</strong>.</p> <p>Here’s the command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>svn merge <span class="nt">-r1001</span>:1000 <span class="nb">.</span>
</code></pre></div></div> <p>In this command:</p> <ul> <li><code class="language-plaintext highlighter-rouge">1001</code> is the newer revision you want to undo or remove.</li> <li><code class="language-plaintext highlighter-rouge">1000</code> is the older, previous revision you want the merge to calculate changes from (i.e., the state you want to revert to).</li> </ul> <p>After executing the merge command, you’ll need to commit the changes:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>svn commit <span class="nt">-m</span> <span class="s2">"Reverting changes introduced in revision 1001."</span>
</code></pre></div></div>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2013-09-01-barbra-liskov/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2013-09-01-barbra-liskov</id><content type="html" xml:base="https://frazik.com/blog/2025/2013-09-01-barbra-liskov/"><![CDATA[<p>Ward Cunningham, the developer of the first wiki and a pioneer in both design patterns and Extreme Programming, gave a great interview on QCon.</p> <p>Around the 18-minute mark, he begins discussing Agile, code review, and knowledge sharing. He elaborates on how Agile helped us (IT professionals) overcome the software crisis, and how other organizational departments now seek to learn lessons from IT and Agile methodologies.</p> <p><a href="http://www.infoq.com/interviews/cunningham-federated-wiki">http://www.infoq.com/interviews/cunningham-federated-wiki</a></p> <p>You can also find an interview with Barbara Liskov from QCon, where she discusses the Liskov Substitution Principle and distributed systems. If you’re unfamiliar with her work, she is a Turing Award winner and, along with her students, was the first to combine abstract data types with operations, essentially paving the way for Object-Oriented Programming (OOP) as we know it today.</p> <p><a href="http://www.infoq.com/interviews/barbara-liskov">http://www.infoq.com/interviews/barbara-liskov</a></p> <p>One must truly respect these seasoned IT professionals. :)</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
</code></pre></div></div>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2014-01-18-book-improving-design/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2014-01-18-book-improving-design</id><content type="html" xml:base="https://frazik.com/blog/2025/2014-01-18-book-improving-design/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>This book, “Refactoring: Improving the Design of Existing Code,” is excellent reading for anyone looking to enhance their coding skills with proven techniques. While many of these concepts are foundational, knowing them by name and understanding their structured application is invaluable for improving code quality and maintainability.</p> <p>Happy reading!</p>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2014-03-12-threads-java/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2014-03-12-threads-java</id><content type="html" xml:base="https://frazik.com/blog/2025/2014-03-12-threads-java/"><![CDATA[<p>There are two primary ways to create new threads in Java. The first, and more common, approach is to implement the <code class="language-plaintext highlighter-rouge">Runnable</code> interface. If you examine the <code class="language-plaintext highlighter-rouge">Thread</code> class’s source code, you’ll see that a <code class="language-plaintext highlighter-rouge">Thread</code> essentially calls the <code class="language-plaintext highlighter-rouge">run()</code> method on a <code class="language-plaintext highlighter-rouge">Runnable</code> object. The second method, which can be simpler in some cases, involves extending the <code class="language-plaintext highlighter-rouge">Thread</code> class directly and overriding its <code class="language-plaintext highlighter-rouge">run()</code> method.</p> <h2 id="using-runnable">Using Runnable</h2> <p>When using the <code class="language-plaintext highlighter-rouge">Runnable</code> interface, you typically create an anonymous inner class (or a separate class) that implements <code class="language-plaintext highlighter-rouge">Runnable</code>, and then pass an instance of it to the <code class="language-plaintext highlighter-rouge">Thread</code> constructor.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Thread</span> <span class="n">secondMethod</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Thread</span><span class="o">(</span><span class="k">new</span> <span class="nc">Runnable</span><span class="o">()</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">index</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">run</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="k">while</span> <span class="o">(</span><span class="n">index</span> <span class="o">&lt;</span> <span class="mi">100</span><span class="o">)</span> <span class="o">{</span>
                <span class="kt">int</span> <span class="n">interval</span> <span class="o">=</span> <span class="o">(</span><span class="kt">int</span><span class="o">)</span> <span class="o">((</span><span class="nc">Math</span><span class="o">.</span><span class="na">random</span><span class="o">()</span> <span class="o">*</span> <span class="mi">500</span><span class="o">)</span> <span class="o">+</span> <span class="mi">500</span><span class="o">);</span>
                <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="n">interval</span><span class="o">);</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">print</span><span class="o">(</span><span class="s">"*"</span><span class="o">);</span>
                <span class="n">index</span><span class="o">++;</span>
            <span class="o">}</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">InterruptedException</span> <span class="n">exc</span><span class="o">)</span> <span class="o">{</span>
            <span class="c1">// Thread was interrupted; handle gracefully if needed.</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">});</span>
</code></pre></div></div> <h2 id="without-runnable-extending-thread">Without Runnable (Extending Thread)</h2> <p>Alternatively, you can create a new thread by extending the <code class="language-plaintext highlighter-rouge">Thread</code> class itself and overriding its <code class="language-plaintext highlighter-rouge">run()</code> method. This approach can be more concise for simple thread implementations.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Thread</span> <span class="n">firstMethod</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Thread</span><span class="o">()</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">index</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">run</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="k">while</span> <span class="o">(</span><span class="n">index</span> <span class="o">&lt;</span> <span class="mi">100</span><span class="o">)</span> <span class="o">{</span>
                <span class="kt">int</span> <span class="n">interval</span> <span class="o">=</span> <span class="o">(</span><span class="kt">int</span><span class="o">)</span> <span class="o">((</span><span class="nc">Math</span><span class="o">.</span><span class="na">random</span><span class="o">()</span> <span class="o">*</span> <span class="mi">500</span><span class="o">)</span> <span class="o">+</span> <span class="mi">500</span><span class="o">);</span>
                <span class="n">sleep</span><span class="o">(</span><span class="n">interval</span><span class="o">);</span> <span class="c1">// 'sleep' can be called directly as this is a Thread instance</span>
                <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">print</span><span class="o">(</span><span class="s">"."</span><span class="o">);</span>
                <span class="n">index</span><span class="o">++;</span>
            <span class="o">}</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">InterruptedException</span> <span class="n">exc</span><span class="o">)</span> <span class="o">{</span>
            <span class="c1">// Thread was interrupted; handle gracefully if needed.</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">};</span>
</code></pre></div></div>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2014-09-06-erasure-bridge-method/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2014-09-06-erasure-bridge-method</id><content type="html" xml:base="https://frazik.com/blog/2025/2014-09-06-erasure-bridge-method/"><![CDATA[<p>“Generic programming is about abstracting and classifying algorithms and data structures. Its goal is the incremental construction of systematic catalogs of useful, efficient, and abstract algorithms and data structures.” — Alexander Stepanov</p> <p>Generic programming concepts are nothing new. Java did not introduce them to the world; languages like Ada, Eiffel, and C++ supported generics even before Java did. In 1988, David Musser and Alexander Stepanov introduced and defined this concept.</p> <p>Java introduced Generics in 2004 with Java 5 and implemented them as <strong>type erasure</strong>. Type erasure consists of the following steps:</p> <ul> <li>Replace all type parameters in generic types with their bounds, or <code class="language-plaintext highlighter-rouge">Object</code> if the type parameters are unbounded. The produced bytecode, therefore, contains only ordinary classes, interfaces, and methods.</li> <li>Insert type casts if necessary to preserve type safety.</li> <li>Generate bridge methods to preserve polymorphism in extended generic types.</li> </ul> <p>From this, you can conclude that generics in Java are purely a compile-time feature. Because of this, generics in Java incur no run-time overhead, which is an important point. It is suspected that they implemented generics as a compile-time correctness feature because of a focus on backward compatibility.</p> <h3 id="demonstrating-type-erasure">Demonstrating Type Erasure</h3> <p>Here is a simple demonstration of replacing generic types with their bounds. In this case, the bound is <code class="language-plaintext highlighter-rouge">Object</code>. While there are better ways to copy an array to a collection (like the <code class="language-plaintext highlighter-rouge">Collections.addAll</code> method), this example is purely for demonstration purposes.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">static</span> <span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="kt">void</span> <span class="nf">array2Coll</span><span class="o">(</span><span class="no">T</span><span class="o">[]</span> <span class="n">a</span><span class="o">,</span> <span class="nc">Collection</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="n">c</span><span class="o">)</span> <span class="o">{</span>
  <span class="k">for</span> <span class="o">(</span><span class="no">T</span> <span class="n">o</span> <span class="o">:</span> <span class="n">a</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">c</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">o</span><span class="o">);</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>After type erasure, the code will look like this:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">array2Coll</span><span class="o">(</span><span class="nc">Object</span><span class="o">[]</span> <span class="n">a</span><span class="o">,</span> <span class="nc">Collection</span> <span class="n">c</span><span class="o">)</span> <span class="o">{</span>
  <span class="k">for</span> <span class="o">(</span><span class="nc">Object</span> <span class="n">o</span> <span class="o">:</span> <span class="n">a</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">c</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">o</span><span class="o">);</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>As you can see, the generic type <code class="language-plaintext highlighter-rouge">T</code> has been replaced with the <code class="language-plaintext highlighter-rouge">Object</code> type (its upper bound). If the bound were something else (for example, <code class="language-plaintext highlighter-rouge">&lt;T extends Comparable&gt;</code>), then the generic type would be replaced by <code class="language-plaintext highlighter-rouge">Comparable</code>.</p> <h3 id="understanding-bridge-methods">Understanding Bridge Methods</h3> <p>Sometimes the compiler creates a synthetic method, called a bridge method, as part of the type erasure process. The next example will explain why and when the compiler creates these methods.</p> <p>Consider the following generic class and its subclass:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="o">{</span>
  <span class="kd">private</span> <span class="no">T</span> <span class="n">data</span><span class="o">;</span>

  <span class="kd">public</span> <span class="nf">Node</span><span class="o">(</span><span class="no">T</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">this</span><span class="o">.</span><span class="na">data</span> <span class="o">=</span> <span class="n">data</span><span class="o">;</span>
  <span class="o">}</span>

  <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setData</span><span class="o">(</span><span class="no">T</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Node.setData"</span><span class="o">);</span>
    <span class="k">this</span><span class="o">.</span><span class="na">data</span> <span class="o">=</span> <span class="n">data</span><span class="o">;</span>
  <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">MyNode</span> <span class="kd">extends</span> <span class="nc">Node</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="o">{</span>
  <span class="kd">public</span> <span class="nf">MyNode</span><span class="o">(</span><span class="nc">Integer</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="kd">super</span><span class="o">(</span><span class="n">data</span><span class="o">);</span>
  <span class="o">}</span>

  <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setData</span><span class="o">(</span><span class="nc">Integer</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"MyNode.setData"</span><span class="o">);</span>
    <span class="kd">super</span><span class="o">.</span><span class="na">setData</span><span class="o">(</span><span class="n">data</span><span class="o">);</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>After type erasure, the compiler will create one synthetic bridge method for the <code class="language-plaintext highlighter-rouge">MyNode</code> class:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Node</span> <span class="o">{</span>
  <span class="kd">private</span> <span class="nc">Object</span> <span class="n">data</span><span class="o">;</span>

  <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setData</span><span class="o">(</span><span class="nc">Object</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Node.setData"</span><span class="o">);</span>
    <span class="k">this</span><span class="o">.</span><span class="na">data</span> <span class="o">=</span> <span class="n">data</span><span class="o">;</span>
  <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">MyNode</span> <span class="kd">extends</span> <span class="nc">Node</span> <span class="o">{</span>
  <span class="kd">public</span> <span class="nf">MyNode</span><span class="o">(</span><span class="nc">Integer</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="kd">super</span><span class="o">(</span><span class="n">data</span><span class="o">);</span>
  <span class="o">}</span>

  <span class="c1">// synthetic bridge method</span>
  <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setData</span><span class="o">(</span><span class="nc">Object</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">setData</span><span class="o">((</span><span class="nc">Integer</span><span class="o">)</span> <span class="n">data</span><span class="o">);</span>
  <span class="o">}</span>

  <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setData</span><span class="o">(</span><span class="nc">Integer</span> <span class="n">data</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"MyNode.setData (Integer)"</span><span class="o">);</span>
    <span class="kd">super</span><span class="o">.</span><span class="na">setData</span><span class="o">(</span><span class="n">data</span><span class="o">);</span>
  <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>In this example, a bridge method was created because the <code class="language-plaintext highlighter-rouge">MyNode</code> class was missing a <code class="language-plaintext highlighter-rouge">setData</code> method that accepts an <code class="language-plaintext highlighter-rouge">Object</code> parameter, which is required for proper inheritance after type erasure. Without this bridge method, we would not have proper polymorphic behavior, and the following example would throw a <code class="language-plaintext highlighter-rouge">ClassCastException</code>:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">MyNode</span> <span class="n">mn</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MyNode</span><span class="o">(</span><span class="mi">5</span><span class="o">);</span>
<span class="nc">Node</span> <span class="n">n</span> <span class="o">=</span> <span class="n">mn</span><span class="o">;</span>
<span class="n">n</span><span class="o">.</span><span class="na">setData</span><span class="o">(</span><span class="s">"Hello"</span><span class="o">);</span> <span class="c1">// This would throw a ClassCastException</span>
</code></pre></div></div> <p>The bridge method <code class="language-plaintext highlighter-rouge">public void setData(Object data)</code> ensures that when <code class="language-plaintext highlighter-rouge">n.setData("Hello")</code> is called, it correctly dispatches to <code class="language-plaintext highlighter-rouge">MyNode</code>’s specialized <code class="language-plaintext highlighter-rouge">setData(Integer data)</code> method, after casting, which then fails with <code class="language-plaintext highlighter-rouge">ClassCastException</code> because “Hello” cannot be cast to <code class="language-plaintext highlighter-rouge">Integer</code>. This preserves the runtime type safety and polymorphism expected from the generic declaration, even though the underlying bytecode uses <code class="language-plaintext highlighter-rouge">Object</code>.</p>]]></content><author><name></name></author></entry><entry><title type="html"></title><link href="https://frazik.com/blog/2025/2022-08-04-serverless-react/" rel="alternate" type="text/html" title=""/><published>2025-09-18T11:53:11+00:00</published><updated>2025-09-18T11:53:11+00:00</updated><id>https://frazik.com/blog/2025/2022-08-04-serverless-react</id><content type="html" xml:base="https://frazik.com/blog/2025/2022-08-04-serverless-react/"><![CDATA[<p>This Terraform example demonstrates how to create a <strong>serverless React application</strong> that utilizes <strong>AWS API Gateway</strong> to invoke an <strong>AWS Lambda</strong> function. The API Gateway is configured to proxy GET requests to the Lambda function, which simply returns “Hello World.”</p> <h3 id="terraform-infrastructure-definition">Terraform Infrastructure Definition</h3> <p>This section contains the actual Terraform infrastructure definition, which includes CORS configuration.</p> <p><a href="https://gist.github.com/spookysleeper/b8ebded1d37f3eeb564e47eaeff8d93f.js">View Terraform Infrastructure Definition Gist</a></p> <h3 id="lambda-function">Lambda Function</h3> <p>This is the Python Lambda function code that will return “Hello World.”</p> <p><a href="https://gist.github.com/spookysleeper/754b0ea994cb2d305dcc781f9aee8998.js">View Lambda Function Gist</a></p>]]></content><author><name></name></author></entry><entry><title type="html">Optimizing Large Language Models A Look at Key Techniques</title><link href="https://frazik.com/blog/2024/modern-techniques-llm-perf/" rel="alternate" type="text/html" title="Optimizing Large Language Models A Look at Key Techniques"/><published>2024-03-13T21:59:00+00:00</published><updated>2024-03-13T21:59:00+00:00</updated><id>https://frazik.com/blog/2024/modern-techniques-llm-perf</id><content type="html" xml:base="https://frazik.com/blog/2024/modern-techniques-llm-perf/"><![CDATA[<p>Large Language Models (LLMs) have revolutionized the AI world, showcasing remarkable capabilities from text generation to code creation. However, like any powerful tool, LLMs offer opportunities for optimization. Researchers are continuously exploring new techniques to enhance LLM performance and unlock their full potential. In this introductory blog post, we’ll explore some of the most promising approaches that are shaping the future of LLMs.</p> <h2 id="1-retrieval-augmented-generation-rag">1. Retrieval-Augmented Generation (RAG)</h2> <p>Imagine an LLM that can not only generate text but also access and leverage relevant information from external sources. This is the core idea behind Retrieval-Augmented Generation (RAG) models. RAG combines an LLM with a retrieval system that fetches information pertinent to the task at hand. The LLM then utilizes this retrieved information to enhance its generation process, leading to more factually accurate and informative outputs.</p> <h2 id="2-chaining-transformers">2. Chaining Transformers</h2> <p>LLMs are often monolithic entities, attempting to tackle entire tasks in a single pass. Chaining Transformers breaks down complex tasks into smaller, more manageable subtasks. Each subtask is handled by a specialized transformer model, and the outputs are sequentially chained together to achieve the final goal. This approach allows for more efficient training and potentially better performance on intricate tasks.</p> <h2 id="3-prompt-engineering">3. Prompt Engineering</h2> <p>Think of prompts as instructions that guide an LLM towards a desired outcome. Prompt engineering focuses on crafting effective prompts that steer the LLM in the right direction. By carefully designing prompts that incorporate task-specific information and desired outcomes, researchers can significantly improve the quality and accuracy of LLM outputs.</p> <h2 id="4-transfer-learning-and-fine-tuning">4. Transfer Learning and Fine-tuning</h2> <p>Pre-trained LLMs have learned a wealth of knowledge from massive datasets. Transfer learning and fine-tuning techniques leverage this pre-trained knowledge as a starting point for new tasks. By fine-tuning an LLM on a task-specific dataset, researchers can significantly reduce training time and improve performance compared to training from scratch.</p> <p>This blog post has only scratched the surface of the exciting advancements in LLM technology. In future posts, we’ll delve deeper into each of these techniques, exploring their specific applications and showcasing their potential to revolutionize various AI domains.</p>]]></content><author><name></name></author><category term="artificial-intelligence,"/><category term="large-language-models"/><category term="LLMs,"/><category term="AI,"/><category term="machine"/><category term="learning,"/><category term="NLP,"/><category term="RAG,"/><category term="prompt"/><category term="engineering,"/><category term="transfer"/><category term="learning,"/><category term="optimization"/><summary type="html"><![CDATA[An introduction to key techniques for optimizing Large Language Models (LLMs), including Retrieval-Augmented Generation (RAG), Chaining Transformers, Prompt Engineering, and Transfer Learning and Fine-tuning.]]></summary></entry><entry><title type="html">Nginx Reverse Proxy for Amazon OpenSearch Kibana with Ansible</title><link href="https://frazik.com/blog/2022/kibana-reverse-proxy/" rel="alternate" type="text/html" title="Nginx Reverse Proxy for Amazon OpenSearch Kibana with Ansible"/><published>2022-10-19T14:34:00+00:00</published><updated>2022-10-19T14:34:00+00:00</updated><id>https://frazik.com/blog/2022/kibana-reverse-proxy</id><content type="html" xml:base="https://frazik.com/blog/2022/kibana-reverse-proxy/"><![CDATA[<h2 id="motivation">Motivation</h2> <p>Amazon OpenSearch Service cluster instances run inside a Virtual Private Cloud (VPC). If you want to access the Kibana interface dedicated to one of these instances, you generally have two options. One is tunneling to an EC2 bastion host, which is relatively straightforward. However, a disadvantage of this approach is the need to share bastion host keys with clients. Another option is to set up a reverse proxy on a bastion host that points to the private OpenSearch Kibana endpoint.</p> <p>This example demonstrates how you can set up access to Kibana using an Nginx reverse proxy and provision it with Ansible.</p> <p>This example represents a basic setup that can serve as a foundation for future improvements. It does not include secure access configurations like SSL/TLS certificates or authentication. It uses HTTP between the client and the proxy server; for a production environment, using HTTPS in this context is highly recommended. While it’s an easier setup, it is also less secure.</p> <h2 id="example">Example</h2> <h3 id="inventory">Inventory</h3> <p>First, you need to define your Ansible inventory with at least one variable (<code class="language-plaintext highlighter-rouge">open_search_endpoint</code>) which should point to your Kibana instance. Notice that we have two EC2 instances in our inventory. One could be for a production environment, and the second for staging, for example.</p> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[opensearch_proxies]</span>
<span class="err">proxy-prod</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">ec2-X-X-X-X.compute-1.amazonaws.com open_search_endpoint="http://your-prod-opensearch-kibana-endpoint.com"</span>
<span class="err">proxy-staging</span> <span class="py">ansible_host</span><span class="p">=</span><span class="s">ec2-Y-Y-Y-Y.compute-1.amazonaws.com open_search_endpoint="http://your-staging-opensearch-kibana-endpoint.com"</span>
</code></pre></div></div> <h3 id="main-playbook">Main Playbook</h3> <p>Next, we define the main Ansible playbook, which is quite straightforward. For it to work, you need to have the configuration files (<code class="language-plaintext highlighter-rouge">default.conf.j2</code> and a basic <code class="language-plaintext highlighter-rouge">nginx.conf</code>) located in your Ansible playbook’s path.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Setup Nginx Reverse Proxy for OpenSearch Kibana</span>
  <span class="na">hosts</span><span class="pi">:</span> <span class="s">opensearch_proxies</span>
  <span class="na">become</span><span class="pi">:</span> <span class="s">yes</span>

  <span class="na">tasks</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Update apt cache (Debian/Ubuntu)</span>
      <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
        <span class="na">update_cache</span><span class="pi">:</span> <span class="s">yes</span>
      <span class="na">when</span><span class="pi">:</span> <span class="s">ansible_os_family == "Debian"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install Nginx (Debian/Ubuntu)</span>
      <span class="na">ansible.builtin.apt</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
      <span class="na">when</span><span class="pi">:</span> <span class="s">ansible_os_family == "Debian"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install Nginx (RedHat/CentOS)</span>
      <span class="na">ansible.builtin.yum</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">present</span>
      <span class="na">when</span><span class="pi">:</span> <span class="s">ansible_os_family == "RedHat"</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Ensure Nginx service is running and enabled</span>
      <span class="na">ansible.builtin.systemd</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">started</span>
        <span class="na">enabled</span><span class="pi">:</span> <span class="s">yes</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Copy default Nginx configuration for Kibana proxy</span>
      <span class="na">ansible.builtin.template</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">default.conf.j2</span> <span class="c1"># This file needs to be in your files/templates directory</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/nginx/sites-available/default</span>
        <span class="na">mode</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0644"</span>
      <span class="na">notify</span><span class="pi">:</span> <span class="s">Reload Nginx</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Enable site by symlinking</span>
      <span class="na">ansible.builtin.file</span><span class="pi">:</span>
        <span class="na">src</span><span class="pi">:</span> <span class="s">/etc/nginx/sites-available/default</span>
        <span class="na">dest</span><span class="pi">:</span> <span class="s">/etc/nginx/sites-enabled/default</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">link</span>
      <span class="na">notify</span><span class="pi">:</span> <span class="s">Reload Nginx</span>

    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Remove default Nginx welcome page config (if present)</span>
      <span class="na">ansible.builtin.file</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/etc/nginx/conf.d/default.conf</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">absent</span>
      <span class="na">notify</span><span class="pi">:</span> <span class="s">Reload Nginx</span>

  <span class="na">handlers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Reload Nginx</span>
      <span class="na">ansible.builtin.systemd</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
        <span class="na">state</span><span class="pi">:</span> <span class="s">reloaded</span>
</code></pre></div></div> <h3 id="default-configuration">Default Configuration</h3> <p>For <code class="language-plaintext highlighter-rouge">default.conf.j2</code>, we are using a simple proxy pass. If you want a more secure connection, this is where you would configure HTTPS. This file should be placed in your <code class="language-plaintext highlighter-rouge">templates</code> directory (e.g., <code class="language-plaintext highlighter-rouge">playbook_directory/templates/default.conf.j2</code>).</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># templates/default.conf.j2</span>
<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">80</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">_</span><span class="p">;</span> <span class="c1"># Listen on all hostnames or specify your domain</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="n">/</span><span class="p">;</span> <span class="c1"># Note the trailing slash to correctly proxy paths</span>
        <span class="kn">proxy_http_version</span> <span class="mf">1.1</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">Upgrade</span> <span class="nv">$http_upgrade</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">Connection</span> <span class="s">'upgrade'</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">Host</span> <span class="nv">$host</span><span class="p">;</span>
        <span class="kn">proxy_cache_bypass</span> <span class="nv">$http_upgrade</span><span class="p">;</span>
        <span class="c1"># Add headers to avoid issues with Kibana</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Real-IP</span> <span class="nv">$remote_addr</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-For</span> <span class="nv">$proxy_add_x_forwarded_for</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-Proto</span> <span class="nv">$scheme</span><span class="p">;</span>

        <span class="c1"># Ensure Kibana URLs are handled correctly if it expects a specific base path</span>
        <span class="c1"># proxy_redirect default; # Might be needed if Kibana redirects</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="nginx-configuration">Nginx Configuration</h3> <p>This typically refers to the main <code class="language-plaintext highlighter-rouge">nginx.conf</code> file, which is often included with the Nginx installation. The key part for our setup is ensuring that <code class="language-plaintext highlighter-rouge">sites-enabled</code> configurations are included. You might not need to explicitly manage this file via Ansible if the default Nginx installation already includes the necessary <code class="language-plaintext highlighter-rouge">include</code> directives. However, if you need to customize global Nginx settings (like <code class="language-plaintext highlighter-rouge">worker_processes</code> or logging), you would place a template for it (e.g., <code class="language-plaintext highlighter-rouge">nginx.conf.j2</code>) in your <code class="language-plaintext highlighter-rouge">templates</code> directory and copy it to <code class="language-plaintext highlighter-rouge">/etc/nginx/nginx.conf</code> with Ansible.</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># /etc/nginx/nginx.conf (Example content for completeness)</span>
<span class="k">user</span> <span class="s">www-data</span><span class="p">;</span>
<span class="k">worker_processes</span> <span class="s">auto</span><span class="p">;</span>
<span class="k">pid</span> <span class="n">/run/nginx.pid</span><span class="p">;</span>
<span class="k">include</span> <span class="n">/etc/nginx/modules-enabled/*.conf</span>;

<span class="k">events</span> <span class="p">{</span>
    <span class="kn">worker_connections</span> <span class="mi">768</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">http</span> <span class="p">{</span>
    <span class="kn">sendfile</span> <span class="no">on</span><span class="p">;</span>
    <span class="kn">tcp_nopush</span> <span class="no">on</span><span class="p">;</span>
    <span class="kn">types_hash_max_size</span> <span class="mi">2048</span><span class="p">;</span>

    <span class="kn">include</span> <span class="n">/etc/nginx/mime.types</span><span class="p">;</span>
    <span class="kn">default_type</span> <span class="nc">application/octet-stream</span><span class="p">;</span>

    <span class="kn">ssl_protocols</span> <span class="s">TLSv1</span> <span class="s">TLSv1.1</span> <span class="s">TLSv1.2</span> <span class="s">TLSv1.3</span><span class="p">;</span>
    <span class="kn">ssl_prefer_server_ciphers</span> <span class="no">on</span><span class="p">;</span>

    <span class="kn">access_log</span> <span class="n">/var/log/nginx/access.log</span><span class="p">;</span>
    <span class="kn">error_log</span> <span class="n">/var/log/nginx/error.log</span><span class="p">;</span>

    <span class="kn">gzip</span> <span class="no">on</span><span class="p">;</span>

    <span class="kn">include</span> <span class="n">/etc/nginx/conf.d/*.conf</span><span class="p">;</span>
    <span class="kn">include</span> <span class="n">/etc/nginx/sites-enabled/*</span><span class="p">;</span> <span class="c1"># This line ensures our 'default' site config is loaded</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="devops,"/><category term="aws"/><category term="ansible,"/><category term="nginx,"/><category term="opensearch,"/><category term="kibana,"/><category term="reverse-proxy,"/><category term="aws,"/><category term="automation,"/><category term="devops"/><summary type="html"><![CDATA[A guide on how to set up access to Amazon OpenSearch Service Kibana using an Nginx reverse proxy, provisioned with Ansible. This includes defining inventory, creating a main playbook, and configuring Nginx for basic proxying.]]></summary></entry></feed>