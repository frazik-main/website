name: Check for broken links

on:
  push:
    branches:
      - master
      - main
    paths:
      - "public/**"
      - "src/**"
      - "**.html"
      - "**.js"
      - "**/*.md"
      - "**.yml"
      - "!.github/workflows/lighthouse-badger.yml"
  pull_request:
    branches:
      - master
      - main
    paths:
      - "public/**"
      - "src/**"
      - "**.html"
      - "**.js"
      - "**/*.md"
      - "**.yml"
      - "!.github/workflows/lighthouse-badger.yml"

jobs:
  link-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Link Checker 🔗
        uses: lycheeverse/lychee-action@v2.1.0
        with:
          fail: true
          # removed md files that include liquid tags
          args: --user-agent 'curl/7.54' --exclude-path README.md --verbose --no-progress './**/*.md' './**/*.html'
